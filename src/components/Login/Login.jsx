import React from 'react';

const Login = () => {
    return (
        <div>
            <h1>please login</h1>
            <button>Sign in with Google</button>
        </div>
    );
};

export default Login;